services:
  - type: web
    name: kobi-depo-vms
    env: node
    plan: free
    buildCommand: cd client && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
